#!/usr/bin/make -f
# vim:ts=4:sw=4
#
# Copyright (c) 2011-2012 Casper Ti. Vector
# Public domain.

PROJECT = $(TITLE)-$(VERSION)
DOC = $(PROJECT)/doc

dist: distclean dist-dir doc-pdf dist-zip

dist-dir: tex/ doc/
	mkdir -p $(PROJECT)/
	cp -r tex/ $(PROJECT)/
	cp -r doc/ $(PROJECT)/

doc-pdf:
	cd $(DOC)/ && make
	mv $(DOC)/readme.pdf $(PROJECT)/$(README).pdf
	cd $(DOC)/ && make distclean

dist-zip: $(PROJECT)
	zip -rmT $(PROJECT).zip $(PROJECT)/

distclean:
	cd doc && make distclean
	rm -rf $(PROJECT)/ $(PROJECT).zip

